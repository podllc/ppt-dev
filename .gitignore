# ============================================================================
# PrescriberPoint Development Environment - Git Ignore
# ============================================================================

# Environment configuration (contains secrets)
.env

# Cloned repositories (managed independently)
repos/*
# But track Claude workspace and instructions if present
!repos/CLAUDE.md
!repos/claude-workspace/

# Docker home directory caches (VS Code extensions, bash history, dotnet tools, etc.)
# Each service has its own .devcache subdirectory - these can get very large (3-4GB each)
.devcache/*
!.devcache/.gitignore
# Allow shared directories for Claude authentication and GitHub CLI
!.devcache/claude-shared/
!.devcache/gh-shared/

# VS Code workspace files (developer-specific)
*.code-workspace
!ppt-dev.code-workspace

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/settings.json
.vscode/launch.json

# Logs
*.log
logs/

# Temporary files
*.tmp
*.swp
*.swo
*~
tmp/
temp/

# Docker volumes
*.data

# Claude Code local settings
.claude/settings.local.json
